{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "id": "https://raw.githubusercontent.com/valueflows/valueflows/master/json-schemas/RecipeProcess.json",
    "title": "RecipeProcess",
    "description": "Specifies the process part of a recipe for use in planning from recipe.",
    "type": "object",
    "properties": {
        "name": {
            "description": "An informal or formal textual identifier for an object. Does not imply uniqueness.",
            "type": "string"
        },
        "note": {
            "description": "A textual comment or description.",
            "type": "string"
        },
        "duration": {
            "description": "The calendar time extent defined for this type of process.",
            "type": "string",
            
            
            TemporalDuration
        },
        "durationMultiplier": {
            "description": "The multiplier for duration based on capacities for the recipe.",
            "type": "number"
        },
        "processClassifiedAs": {
            "description": "References a concept in a common taxonomy or other classification scheme for purposes of categorization.",
            "type": "array",
            "items": { 
                "type": "string",
                "$comment": "If possible, this should be a uri reference to a shared classification on the web."
            }
        }
    },
    "required": ["name"]
}
